{
  "id": "2hr",
  "title": "A factory deploy does not say which bundle it is deploying",
  "description": "The factory deploys from a bundle embedded in the ticfac binary (go:embed), staged to ~/.tick/factory/ticfac/. That is deliberate - deploy-factory.yml says the deploy runs 'from the bundle embedded in the exact commit being released', so there is one deploy path.\n\nBut the deploy never says WHICH bundle it is using, and the consequence is a silent loop. On 2026-09-20 I fixed cloudflare/wrangler.toml in the working tree, redeployed, and got a byte-identical failure. Nothing indicated my edit had not been read. The staged copy at ~/.tick/factory/ticfac/cloudflare/wrangler.toml still held the old file, because the fix only takes effect after the BINARY is rebuilt.\n\nAn operator editing config and redeploying gets the same error forever with no clue why. The fix that works - rebuild the binary - is not suggested by anything in the output.\n\nThe deploy already prints 'ticfac: dev' and 'image tk: built from \u003csha\u003e'. It should also print the bundle's provenance: the commit the embedded bundle came from, and a clear statement that the working tree is NOT the source. 'ticfac: dev' is actively unhelpful here - a dev build is exactly the case where the operator is most likely to be editing the bundle's source.\n\nRelated: 63i, the migration ordering bug this loop was hiding.",
  "notes": "2026-09-21 08:52 - prs (PR #25) makes this worse in a specific way, and the fix belongs here rather than in a new tick - same acceptance sentence.\n\nA deploy now carries TWO PROVENANCES:\n  - the Dockerfile and the scripts come from the //go:embed'ed tree, i.e. the commit the RUNNING BINARY was built from;\n  - the two staged ticfac binaries come from 'go build' over the CURRENT WORKING TREE.\n\nIn a release those coincide, which is the property prs wanted. In the DEV case - the exact loop this tick was filed about - they diverge, and both halves are labelled 'dev'.\n\nThat is the mirror image of this tick's original symptom. There, an edit to bundle source was silently ignored because the deploy read the embedded copy. Here, an edit takes effect in ONE HALF and not the other, silently. Both are the deploy not saying what it is deploying.\n\nFix, suggested by the agent that found it: stamp the staged binaries with the source commit and a dirty flag rather than just opts.Version, and have the deploy print both provenances on separate lines. TICFAC_VERSION is the natural carrier - it already reaches the container, and the image now has a working 'ticfac version' to read it back.",
  "status": "open",
  "priority": 2,
  "type": "task",
  "owner": "peter@engelbrecht.dk",
  "acceptance_criteria": "A deploy states the commit its embedded bundle came from and that the working tree is not the source, so an operator who edits bundle source and redeploys can see why nothing changed",
  "created_by": "peter@engelbrecht.dk",
  "created_at": "2026-09-20T18:52:37.968555Z",
  "updated_at": "2026-09-21T06:52:53.18877Z"
}